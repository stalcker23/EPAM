@using Epam.Forums.WebUI.Models;
@{
    if (IsPost)
    {
        Response.SetStatus(HttpStatusCode.BadRequest);
        return;
    }

    int id = int.Parse(Request["Id"]);
    var photo = Common.ImageBll.GetSmallPhoto(id);

    Response.WriteBinary(photo.Data, photo.ContentType);
}